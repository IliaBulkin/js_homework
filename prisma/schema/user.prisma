model User {
  id        Int       @id @default(autoincrement()) 
  username  String
  email     String 
  password  String
  role      String
  posts     Post[]    @relation("UserPosts")    // это я для себя, связь с моделью Post
  comments  Comment[] @relation("UserComments") // связь с моделью Comment
}
